services:
  - type: web
    name: semantic-world-explorer
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
    envVars:
      - key: VOYAGE_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.9"
